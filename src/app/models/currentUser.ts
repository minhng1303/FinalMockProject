export interface currentUser {
        id: number;
        email: string;
        createdAt: string;
        updatedAt: string;
        username: string;
        bio?: any;
        image?: any;
        token: string;
    }
  
  